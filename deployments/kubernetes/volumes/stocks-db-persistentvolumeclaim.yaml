apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: stocks-db
  name: stocks-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
